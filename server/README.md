# Realtime Data Processing Pipeline - Server

This folder contains a minimal TypeScript Express server used for the realtime data processing pipeline.

Quick start

1. Install dependencies:

   npm install

2. Run in development mode:

   npm run dev

3. Build and start:

   npm run build
   npm start

The server exposes a health check at GET /health
